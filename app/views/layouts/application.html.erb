<!DOCTYPE html>
<html>
	<head>
		<title><%= title %></title>
		<%= render 'layouts/stylesheets' %>
	</head>
	<body>
		<%= render 'layouts/header' %>
			<%= yield %>
		<div class = 'clear'></div>
		<footer>
		
		</footer>
		<%= debug(params) if Rails.env.development? %>
	</body>
</html>